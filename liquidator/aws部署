（cursor AI：）
1. 准备AWS Ubuntu服务器

Amazon Linux 2023

# 更新系统
sudo dnf update -y

# 安装必要的软件包
sudo dnf install -y git curl wget unzip

# 安装Docker
sudo dnf install -y docker

# 启动Docker服务
sudo systemctl start docker
sudo systemctl enable docker

# 将当前用户添加到docker组
sudo usermod -aG docker $USER

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 重新登录或重新加载组权限
newgrp docker

2. 从GitHub克隆项目
git clone git@github.com:0x-zeros/suilend-fe-public.git



3.
启动：
docker-compose up -d --build


下一次这个就可以了：
docker-compose up -d




使用 docker-compose up -d 当：
    服务已经正常运行，只是重启
    没有代码更改
    快速测试


使用 docker-compose up -d --build 当：
    修改了源代码
    更新了package.json依赖
    第一次部署
    怀疑镜像有问题需要重新构建






【构建和启动服务】

# 进入项目目录
cd suilend-fe-public

# 确保在liquidator目录下
pwd
# 应该显示: /path/to/suilend-fe-public/liquidator


# 停止服务（保留容器）
docker-compose stop

# 停止任何现有服务 （删除所有容器， 删除容器日志 等）
docker-compose down

# 重新启动
docker-compose start


# 构建和启动服务
docker-compose up -d

# 构建并启动服务
docker-compose up -d --build

# 查看服务状态
docker-compose ps


[检查服务状态]
# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 查看特定服务的日志
docker-compose logs -f dispatcher
docker-compose logs -f worker

[监控服务运行]
# 实时查看日志
docker-compose logs -f

# 查看最近的日志
docker-compose logs --tail=50

# 检查Redis连接
docker-compose exec redis redis-cli ping


[如果遇到问题，可以手动调试]
# 进入容器调试
docker-compose exec dispatcher sh
docker-compose exec worker sh

# 在容器内检查
ls -la
npm list
node --version


